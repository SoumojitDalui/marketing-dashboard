meta {
  name: Validation — missing required parameter
  type: http
  seq: 7
}

get {
  url: https://eulerity-hackathon.appspot.com/v1/metrics-insights?startDate=2026-06-27&endDate=2026-07-10
  body: none
  auth: none
}

tests {
  test("returns MISSING_PARAM when network is omitted", function () {
    expect(res.getStatus()).to.equal(400);
    expect(res.getBody().error).to.equal("MISSING_PARAM");
    expect(res.getBody()).to.have.property("message");
  });
}
