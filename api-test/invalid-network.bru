meta {
  name: Validation — invalid network
  type: http
  seq: 5
}

get {
  url: https://eulerity-hackathon.appspot.com/v1/metrics-insights?network=tiktok&startDate=2026-06-27&endDate=2026-07-10
  body: none
  auth: none
}

tests {
  test("returns the documented invalid-network error", function () {
    expect(res.getStatus()).to.equal(400);
    expect(res.getBody().error).to.equal("INVALID_NETWORK");
    expect(res.getBody()).to.have.property("message");
  });
}
